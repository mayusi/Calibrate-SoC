package io.github.mayusi.calibratesoc.data.prefs

/** User-selectable clock display unit. */
enum class ClockUnit { MHZ, GHZ }

/** User-selectable temperature unit. */
enum class TempUnit { CELSIUS, FAHRENHEIT }
